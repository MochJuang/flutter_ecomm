import 'package:flutter/material.dart';

class BodySI extends StatelessWidget {
  const BodySI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
